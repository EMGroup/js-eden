rectangle is ["rectangle",x,y,width,height];

rect1 is rectangle{x = 81, y = 35, width = 21, height = 19};

scaler is [shape[1],shape[2],shape[3],shape[4]*scale,shape[5]*scale];

rect1_scaled is scaler{shape = rect1, scale = 2};

rect_map is Rectangle(shape[2], shape[3], shape[4], shape[5]);

b is rect_map{shape = rect1 };
e is rect_map{shape = rect1_scaled };
picture is [b,e];
